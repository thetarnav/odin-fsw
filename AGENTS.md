Use git for version control as you make changes.
Test the api locally (linux arch os).
Other oses will be tested later after it's finished.
Continue until the library is complete and working.
If you must change the plan—something unexpected requires that—then edit the plan.md document accordingly and keep it in sync with the implementation state.
Only touch files inside this repository. If you need to create any temporaty files, do it here and remove them afterwards.
